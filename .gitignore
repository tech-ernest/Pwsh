# Local config with API keys — never commit
config/settings.json

# Runtime state and business records
data/*
!data/.gitkeep

# Backups (contain the ledger and config secrets)
backups/

# Scratch
*.log
